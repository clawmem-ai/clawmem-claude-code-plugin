{
  "name": "clawmem-claude-code-plugin",
  "version": "0.2.0",
  "private": true,
  "description": "ClawMem durable memory plugin for Claude Code. Ships Claude-Code-specific hooks; memory / issue / collaboration tools are served by the shared clawmem-mcp-server binary.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:e2e": "bash ./e2e/claude-cli-smoke.sh",
    "test:e2e:staging": "bash ./e2e/claude-cli-smoke.sh --target=staging"
  },
  "engines": {
    "node": ">=18"
  }
}
